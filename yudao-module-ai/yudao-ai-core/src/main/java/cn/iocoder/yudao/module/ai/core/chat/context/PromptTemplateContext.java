package cn.iocoder.yudao.module.ai.core.chat.context;

public class PromptTemplateContext {

    public static String TAXONOMY_SYSTEM = """
        请根据以下参考数据和内容摘要，生成结构化分类与标签的JSON数据。
        
        ## 核心约束（优先级最高）
        **重要：只有当内容摘要与某个分类/标签的语义匹配度 ≥ 96% 时，才生成该数据项。**
        如果没有任何分类/标签满足此阈值，请返回空结构（空数组、null或默认无值状态），绝不要强行匹配。
        
        ## 参考数据结构（请严格遵循此结构）
        ```json
        {format}
        ```
        
        ## 参考数据结构的引用数据（请使用该数据构建数据结构）
        ```json
        {citation}
        ```
        
        ## 内容摘要
        {summary}
        
        ## 生成要求（严格按顺序执行）
        1. **匹配度验证**：逐项判断内容摘要是否与引用数据中的分类/标签高度匹配（语义相似度 ≥ 90%）
        2. **数据结构**：仅当满足步骤1的阈值时，才填充对应字段；结构必须与参考数据的JSON完全一致（字段名、类型、嵌套层级）
        3. **置信度字段**：如果数据结构中包含confidence/score/匹配度相关字段，请填写具体数值（0-1或0-100）；如果没有但您需要记录，请在输出前增加一个`score`字段（值为0.0~1.0）
        4. **数组处理**：如果参考数据结构中包含数组，只生成满足匹配度条件的数组元素；若无符合条件的元素，返回空数组`[]`
        5. **ID生成**：如果引用数据中有id字段，请使用相应的id值；若未找到匹配项，不要生成该条目
        6. **禁止幻觉**：不要发明、推断或补充内容摘要中未明确支持的分类/标签
        7. **完整性校验**：所有非匹配项应被跳过（不输出），而不是填充默认值
        
        ## 匹配度自检清单（内部思考，不输出）
        - 语义相似度 ≥ 90%
        - 语义向量距离 ≤ 0.1（假设1为最大差异）
        - 无反向语义冲突
        - 业务语境一致（如电商标签不用于医疗内容）
        """;

    public static String TAXONOMY_USER = """
        根据系统提示中的参考数据结构和约束条件，为以下文档摘要内容生成分类标签：
        
        {summary}
        
        要求：
        - 严格遵守匹配度阈值（≥96%）
        - 无匹配项时返回空结构
        - 保持JSON格式与参考数据一致
        - 仅输出JSON格式的结果
        """;

    public static String JSON_USER = """
           请根据以下参考数据和引用数据，生成结构化的JSON数据。
           ## 参考数据结构（请严格遵循此结构）
           ```json
           {format}
           ```
           ## 参考数据结构的引用数据（请使用该数据构建数据结构）
           ```json
           {context}
           ```
           
           ## 生成要求
           1. **数据结构**：必须与参考数据的JSON结构完全一致（字段名、类型、嵌套层级）
           2. **数据填充**：根据`参考数据结构的引用数据`提取相关信息来填充数据
           3. **风格保持**：如果参考数据中有特定的命名风格、格式规范，请保持一致
           4. **数组处理**：如果参考数据结构中包含数组，请生成合适数量的数组元素
           5. **ID生成**：如果引用数据中有id字段，请使用相应的id值
           6. **完整性**：确保所有字段都有合理的值，不要遗漏任何字段
           """;
}
